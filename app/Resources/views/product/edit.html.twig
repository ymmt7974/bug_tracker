{% extends 'base.html.twig' %}

{% block body %}
    <h1>Product edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" class="btn btn-primary" value="Update" />
    {{ form_end(edit_form) }}

    <hr>

    <a href="{{ path('product_index') }}" class="btn btn-default" role="button">Back to the list</a>
    {{ form_start(delete_form, {'attr': {'style': 'display: inline'}}) }}
        <input type="submit" class="btn btn-danger" role="button" value="Delete">
    {{ form_end(delete_form) }}{% endblock %}
